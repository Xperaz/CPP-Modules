#ifndef SCALAR_HPP
# define SCALAR_HPP

# include <iostream>
# include <string>
#include <sstream>

class Scalar
{

	public:

		Scalar();
		// Scalar( Scalar const & src );
		~Scalar();

		// Scalar &		operator=( Scalar const & rhs );

	private:

};

int	is_double(std::string arg);

#endif /* ********************************************************** SCALAR_H */